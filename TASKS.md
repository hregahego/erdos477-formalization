# TASKS — Erdős 477 formalization

Append-only work-delegation log for 4 parallel worker agents. The Plan agent
appends one `## Iteration N` block per loop iteration. Each block has a one-line
goal then `Agent k: ...` lines (one per ACTIVE worker; inactive agents are
omitted). NEVER edit or delete an existing block.

---

## Iteration 1
First proving batch after the audited SETUP freeze: discharge the two independent milestones (Stage Greedy, Stage Elementary), the Stage Cofactor bounds, and the Heath-Brown specialization bridge in Stage BadShift. (Cofactor is assigned now even though the BLUEPRINT order chart draws it after Elementary: its three lemmas C1–C3 use no Elementary lemma — only the frozen `Qcof` and Mathlib — so its true prerequisites are all ✅. The BadShift task is ONLY the axiom-specialization bridge lemma, which needs nothing beyond the frozen axiom; `badShift_bound` itself stays untouched until ParamExclusion is done.)

Agent 1: OWNS `Erdos477/Proofs/Greedy/Basic.lean` (and nothing else). Produce `Erdos477.greedy_tiling_proof`, a sorry-free theorem whose statement is CHARACTER-EXACT the frozen `greedy_tiling` type from `Erdos477/Theorems.lean` (abstract `B : Set ℤ`, hypothesis `H` over ∀ finite `C ⊆ ℤ\B` with the abstract difference set `{d | ∃ x ∈ B, ∃ y ∈ B, d = x - y}`, conclusion the full `∃!` over the pair `ab : ℤ × ℤ`). Follow SKETCH.md §7 (L4.1) exactly: enumerate ℤ via `Denumerable ℤ`; build `Aseq : ℕ → Finset ℤ` by recursion with invariants (I1) D-separatedness and (I2) coverage of the first j enumerated integers; in Case 2 apply `H` to `C_j = (Aseq j).image (n_j - ·)`, set `a_j = n_j - b_j`; establish `a_j ∉ Aseq j` via `0 ∈ B−B` (needs `B ≠ ∅`, from `H` applied to `C = ∅`) and both difference directions via the abstract symmetry of `{d | ∃ x ∈ B, ∃ y ∈ B, d = x - y}` (swap witnesses — do NOT use `Dset_neg_mem`, this theorem is generic in `B`); take `A = ⋃ j, Aseq j`, prove covering from (I2) + monotonicity of `Aseq`, uniqueness from D-separatedness (SKETCH §7 "The complement", including the pitfall list §9.5 items 2 and 9). May use: frozen `Defs.lean` (import only; nothing from other stages — this stage is self-contained). Respect the BLUEPRINT "Cheat watch (Stage Greedy)" box: full `∃!` (existence AND uniqueness), `H` stays universally quantified, no swapping in `Dset`. Do NOT edit `Defs.lean`, `Theorems.lean`, `Discharge.lean`, `Solution.lean`, or any other agent's file. No `sorry`, no `native_decide`; `#print axioms Erdos477.greedy_tiling_proof` must show at most `{propext, Classical.choice, Quot.sound}`. Log 🔧/✅/⚠️ entries in PROGRESS.md per BLUEPRINT Part −1 §4 (claim first, report `#print axioms` output in `Check:`).

Agent 2: OWNS `Erdos477/Proofs/Elementary/Basic.lean` (and nothing else). Produce sorry-free: (a) `Erdos477.Dset_neg_mem_proof` with CHARACTER-EXACT the frozen `Dset_neg_mem` type (`{d : ℤ} (hd : d ∈ Dset) : -d ∈ Dset`) — proof by swapping the witnesses `(u,v) ↦ (v,u)` per SKETCH §2 L0.2, NO odd-power sign manipulation; (b) `Erdos477.pow13_injective_proof` with CHARACTER-EXACT the frozen `pow13_injective` type (`Function.Injective (fun m : ℤ => m ^ 13)`) — genuine injectivity on ALL of ℤ via `Odd.pow_right_injective`/`Odd.strictMono` (locate the exact Mathlib name; 13 is odd), per SKETCH §2 L0.3; (c) the support lemmas of BLUEPRINT Stage Elementary E3, in `namespace Erdos477`: `zero_mem_Bset : (0 : ℤ) ∈ Bset` (witness `m = 0`, L0.1), `ne_zero_of_notMem_Bset : c ∉ Bset → c ≠ 0` (L0.5), `badShift_iff : c - t ^ 13 ∈ Dset ↔ t ^ 13 - c ∈ Dset` (L0.6, two applications of (a)), and the odd-power corollary `pow13_eq_neg : x ^ 13 = -(y ^ 13) → x = -y` for `x y : ℤ` (rewrite `-(y^13) = (-y)^13`, then (b)) — used later by ParamExclusion and Assembly; also `rat_pow13_int : ∀ (c : ℤ) (d : ℚ), d ^ 13 = c → ∃ m : ℤ, (m : ℚ) = d` or an equivalent rendering of L0.4 (rational 13th roots of integers are integers, via `ℤ` integrally closed in `ℚ` / rational root theorem) — Route A (mandated by USER_NOTES.md) WILL need it, unlike Route B. Respect the BLUEPRINT "Cheat watch (Stage Elementary)" box: no `decide` over a finite range, no restriction to `m ≥ 0`. Same file-ownership, axiom-hygiene (`#print axioms` = standard three at most), and PROGRESS.md logging rules as Agent 1.

Agent 3: OWNS `Erdos477/Proofs/Cofactor/Basic.lean` (and nothing else). Produce sorry-free, each with CHARACTER-EXACT the corresponding frozen type from `Theorems.lean`: (a) `Erdos477.pow13_sub_pow13_factor_proof` (`u ^ 13 - v ^ 13 = (u - v) * Qcof u v` over ℤ) — unfold `Qcof` to the `range 13` sum and use `geom_sum₂_mul`-family lemmas or `Finset.sum` telescoping / `ring` after expansion, per BLUEPRINT C1; (b) `Erdos477.cofactor_lower_bound_proof` (`(1 / 2 : ℝ) * max |u| |v| ^ 12 ≤ Qcof u v`, ∀ u v : ℝ) — follow SKETCH §4 L1.1 exactly: symmetry `Qcof u v = Qcof v u` by reindexing `i ↦ 12 - i`, WLOG `|v| ≤ |u|`; `u = 0` case trivial; else `s := v/u ∈ [-1,1]`, `Qcof u v = u^12 * ∑_{j<13} s^j`, and `∑_{j<13} s^j ≥ 1/2` split on `0 ≤ s` (j=0 term) vs `s < 0` (geometric identity `(1-s)*∑_{j<13} s^j = 1 - s^13`, `s^13 < 0` as 13 odd, `0 < 1-s ≤ 2`); the constant is EXACTLY 1/2; (c) `Erdos477.pow13_gap_proof` (frozen `pow13_gap` type, hypothesis `u ≠ v` only) — cast to ℝ, factor via (a) (`push_cast` commutes with the polymorphic `Qcof`), `|u - v| ≥ 1` for distinct integers, `Qcof ≥ 0` from (b), per BLUEPRINT C3. May use: frozen `Defs.lean` only (no Elementary lemmas needed — C1–C3 are self-contained). Respect the BLUEPRINT "Cheat watch (Stage Cofactor)" box: `cofactor_lower_bound` is ∀ real `u v`, not integers; equality factorization, not a one-sided bound; guardrail examples `Qcof (1:ℝ) (-1) = 1` etc. Same file-ownership, axiom-hygiene (standard three at most — the HB axiom must NOT appear), and PROGRESS.md logging rules as Agent 1.

Agent 4: OWNS `Erdos477/Proofs/BadShift/Basic.lean` (and nothing else). Task: the Heath-Brown SPECIALIZATION BRIDGE mandated by USER_NOTES.md ("axiomatize the general, derive the specific") and recorded as a proof obligation in PROGRESS.md's 📝 SETUP decision — the sketch's conditional diagonal-13 "AXIOM HB" (SKETCH §3, "recommended Lean form") proved as a LEMMA from the frozen general axiom `Erdos477.heath_brown_diagonal_13`. In `namespace Erdos477` produce sorry-free: (a) `diag13Form : MvPolynomial (Fin 3) ℤ := X 0 ^ 13 + X 1 ^ 13 + X 2 ^ 13` (support def, in Proofs — NOT in Defs.lean) with `diag13Form_isHomogeneous : diag13Form.IsHomogeneous 13` and `diag13Form_nonsingular : IsNonsingularForm diag13Form` (partials are `13 * X i ^ 12`; over ℂ, `13 * x_i ^ 12 = 0 → x_i = 0`); (b) `diag13_eval : ∀ x : Fin 3 → ℤ, MvPolynomial.eval x diag13Form = x 0 ^ 13 + x 1 ^ 13 + x 2 ^ 13` and the aeval analogue for `p : Fin 3 → Polynomial ℤ` (needed to translate `IsParamOfDegLE` into the `p₁^13 + p₂^13 + p₃^13 = C M` shape); (c) the bridge lemma `hb_diagonal_conditional (M : ℤ) (hM : M ≠ 0) (hexcl : ∀ p₁ p₂ p₃ : Polynomial ℤ, p₁ ^ 13 + p₂ ^ 13 + p₃ ^ 13 = Polynomial.C M → p₁.natDegree ≤ 1 → p₂.natDegree ≤ 1 → p₃.natDegree ≤ 1 → p₁.natDegree = 0 ∧ p₂.natDegree = 0 ∧ p₃.natDegree = 0) : ∃ K : ℝ, 1 ≤ K ∧ ∀ X : ℝ, 1 ≤ X → (({x : Fin 3 → ℤ | MvPolynomial.eval x diag13Form = M ∧ ∀ i, |(x i : ℝ)| ≤ X}).ncard : ℝ) ≤ K * X ^ ((10 : ℝ) / 13)`. Proof path (SKETCH §3 "why this form is a faithful consequence", points 1–4, minus the ε-sign step which is unnecessary since the frozen axiom takes arbitrary `N ≠ 0`): apply `heath_brown_diagonal_13 diag13Form 13 (by norm_num) diag13Form_isHomogeneous diag13Form_nonsingular (cN := |M|)`; note `(13 : ℕ) / 10 = 1`; instantiate at `N := M`, and discharge `|(M:ℝ)| ≤ |M| * X` from `1 ≤ X`; then show `HBSolutionSet diag13Form M 1 X = {x | eval x = M ∧ ∀ i, |xᵢ| ≤ X}` because `hexcl` makes `IsParamOfDegLE diag13Form M 1 p` contradictory for every `p` (its `∀ i, natDegree ≤ 1` + the aeval identity feed `hexcl`, whose conclusion `∀ i, natDegree = 0` contradicts the `¬ ∀ i, natDegree = 0` clause), hence `¬ LiesOnParamOfDegLE …` holds for every `x` and the exclusion clause is vacuously true; conclude with `Set.ncard` congruence. Do NOT attempt `badShift_bound` itself (it needs `no_linear_param`, not yet available); do NOT restate the bridge with any weakened hypothesis. May use: frozen `Defs.lean` only. `#print axioms Erdos477.hb_diagonal_conditional` must show exactly the standard three PLUS `Erdos477.heath_brown_diagonal_13` (this lemma legitimately carries the axiom; `brownawell_masser_P1_four_term` must NOT appear). Same file-ownership and PROGRESS.md logging rules as Agent 1.

## Iteration 2
Clear REVIEW.md Iteration-1 follow-ups and open the Route-A ParamExclusion front: wire Discharge/Solution for the six proved frozen theorems (+ the Assembly bridge `Dset_eq_Bset_sub`), build the two independent halves of the Route-A infrastructure (Stage ParamExclusion, SKETCH §5.2, mandated by USER_NOTES.md), and reduce Stage BadShift B1 to a single application of the future `no_linear_param` via a conditional support lemma. Advances Stages ParamExclusion, BadShift, Assembly (bridge only), Discharge/Solution.

Agent 1: OWNS `Erdos477/Solution.lean`, `Erdos477/Discharge.lean`, and `Erdos477/Proofs/Assembly/Basic.lean` (and nothing else — do NOT touch `Erdos477.lean`, `Defs.lean`, `Theorems.lean`, or any `Proofs/` file of another agent). Three tasks. (a) In `Erdos477/Solution.lean`, `namespace Erdos477.Solution`, add VERBATIM restatements of exactly the six proved frozen theorems — `Dset_neg_mem`, `pow13_injective`, `pow13_sub_pow13_factor`, `cofactor_lower_bound`, `pow13_gap`, `greedy_tiling` — each as `theorem <name> : <frozen statement copied character-exact from Erdos477/Theorems.lean> := <name>_proof`, where the `_proof` declarations live in `Erdos477/Proofs/{Elementary,Cofactor,Greedy}/Basic.lean` (all ✅ per PROGRESS.md; REVIEW.md Iteration 1 already machine-verified these six restatements compile). Do NOT restate the four unproved names (`no_linear_param`, `badShift_bound`, `criterion_for_B`, `erdos_477`). (b) In `Erdos477/Discharge.lean`, add the six no-drift gates `example : @Erdos477.<name> = @Erdos477.<name>_proof := rfl` for the same six names (read `scripts/verify.sh` Checks 4–5 first and match exactly the shape it greps for). (c) In `Erdos477/Proofs/Assembly/Basic.lean`, `namespace Erdos477`, prove the support lemma `Dset_eq_Bset_sub : Dset = {d : ℤ | ∃ x ∈ Bset, ∃ y ∈ Bset, d = x - y}` (BLUEPRINT Part −1 §2 D2 modeling note and Stage Assembly A2; both inclusions by witness manipulation: `u^13 − v^13` with `x := u^13 ∈ Bset`, `y := v^13 ∈ Bset`, and conversely) — this is the `Dset` ↔ abstract `B − B` bridge that `erdos_477` will need to apply `greedy_tiling` to `Bset`; do NOT attempt `criterion_for_B` or `erdos_477` (their prerequisite `badShift_bound` is not ✅). May use anything marked ✅ in PROGRESS.md. Build gates: `lake build Erdos477.Proofs.Assembly.Basic Erdos477.Solution Erdos477.Discharge` clean; if a build failure originates in a file another agent is concurrently editing (`ParamExclusion/*`, `BadShift/Basic.lean`), wait for their ✅/⚠️ PROGRESS.md entry and rebuild before reporting. `#print axioms` for every `Solution.*` restatement must stay within {propext, Classical.choice, Quot.sound} (none of these six may carry either custom axiom); `Dset_eq_Bset_sub` likewise standard three at most. No `sorry`, no `native_decide`. Log 🔧 claim / ✅ per BLUEPRINT Part −1 §4.

Agent 2: OWNS `Erdos477/Proofs/ParamExclusion/Basic.lean` (and nothing else). Task: the RatFunc order / S-unit / height toolbox — the analytic half of Route A (SKETCH §5.2.1–5.2.2 "Case A/B" ingredients; USER_NOTES.md mandates Route A, and the BM specializations are proof obligations per the 📝 SETUP decision in PROGRESS.md). Work in `namespace Erdos477` against the frozen defs `ordAtP1`, `IsSUnitP1`, `projHeightP1` (Erdos477/Defs.lean:161–180; `Option k` models ℙ¹, `none` = ∞); state everything over a general `{K : Type*} [Field K]` adding `[IsAlgClosed K]`/`[CharZero K]` only where needed (the spine will instantiate `K := AlgebraicClosure ℚ` next iteration). Produce sorry-free (names indicative — keep them stable and log the final list): (a) representation-independence workhorse `ordAtP1_eq_of_div (p q : Polynomial K) (hp : p ≠ 0) (hq : q ≠ 0) (f : RatFunc K) (hf : f = algebraMap _ _ p / algebraMap _ _ q) : ∀ a : K, ordAtP1 (some a) f = (p.rootMultiplicity a : ℤ) - (q.rootMultiplicity a : ℤ)` (reduce to the coprime num/denom via cancellation of the gcd and `Polynomial.rootMultiplicity_mul`), plus `ordAtP1_none_eq (…) : ordAtP1 none f = (q.natDegree : ℤ) - p.natDegree`; (b) consequences: `ordAtP1_C (a : K) (ha : a ≠ 0) : ∀ P, ordAtP1 P (RatFunc.C a) = 0`, `ordAtP1_algebraMap (p : Polynomial K) (hp : p ≠ 0)` (finite point = `rootMultiplicity`, `none` = `-p.natDegree`), `ordAtP1_mul` and `ordAtP1_pow` for nonzero arguments; (c) constancy criteria: `exists_C_of_forall_ordAtP1_eq_zero (f : RatFunc K) (hf : f ≠ 0) (h : ∀ P, ordAtP1 P f = 0) : ∃ a : K, f = RatFunc.C a` (over `[IsAlgClosed K]`: num and denom have no roots hence are constants) and the SKETCH §5.2.2 "Case C/D orders argument" `exists_C_of_pow13_eq_C (f : RatFunc K) (a : K) (ha : a ≠ 0) (h : f ^ 13 = RatFunc.C a) : ∃ b : K, f = RatFunc.C b` (from `13 * ordAtP1 P f = 0` at every `P`); (d) S-unit constructors: `RatFunc.C a` (`a ≠ 0`) is an `S`-unit for every `S`; `algebraMap (Polynomial K) (RatFunc K) p` is an `S`-unit whenever `p ≠ 0`, `none ∈ S`, and every root of `p` is in `S` (as `some`-points); closure of `IsSUnitP1 S` under `*`, `^`, and `-·`; (e) the height computation of SKETCH §5.2.1 (dehomogenized): `projHeightP1_algebraMap {r : ℕ} (hr : 0 < r) (q : Fin r → Polynomial K) (hq : ∀ i, q i ≠ 0) (i₀ : Fin r) (h₀ : (q i₀).natDegree = 0) : projHeightP1 (fun i => algebraMap _ _ (q i)) = ((Finset.univ.sup fun i => (q i).natDegree : ℕ) : ℤ)` — at a finite point every `ordAtP1` is ≥ 0 and the `i₀` term is 0, so the `⨅` is 0 and the `finsum` has support inside `{none}` (use `finsum_eq_single`/`finsum_mem_support`-style lemmas); at `none` the `⨅ i, -(natDegree (q i))` is `-(sup natDegree)`; (f) the |S| count: for `S := (p.roots.toFinset.image some) ∪ {none}` (`p ≠ 0`), `(S.card : ℤ) ≤ p.natDegree + 1` (via `Polynomial.card_roots'`/`Multiset.toFinset_card_le`), stated so the spine can bound `|S| ≤ 3e + 1` for `p := p₁*p₂*p₃`; (g) the nonconstancy bridge `algebraMap_polynomial_eq_C_iff : algebraMap (Polynomial K) (RatFunc K) p = RatFunc.C a ↔ p = Polynomial.C a` (RatFunc.algebraMap is injective; `RatFunc.algebraMap_C`). Respect the BLUEPRINT "Cheat watch (Stage ParamExclusion)" box: these are support lemmas — do NOT state or prove `no_linear_param` itself this iteration, do NOT add hypotheses to any frozen statement, and do NOT invoke Mathlib's Mason–Stothers (`Polynomial.abc`) anywhere. May use: frozen `Defs.lean`, ✅ Elementary lemmas. `#print axioms` on every lemma: standard three at most (the BM axiom is INVOKED only by next iteration's spine; it must NOT appear here, nor may `heath_brown_diagonal_13`). Build gate: `lake build Erdos477.Proofs.ParamExclusion.Basic` clean. Log 🔧 claim first, then ✅ with the exact final lemma-name list in `Next:` so the iteration-3 spine agent can import them.

Agent 3: OWNS `Erdos477/Proofs/ParamExclusion/Degenerate.lean` (NEW file) and the one-line import addition to `Erdos477.lean` (and nothing else — you are the ONLY agent allowed to touch `Erdos477.lean` this iteration; append `import Erdos477.Proofs.ParamExclusion.Degenerate` after the existing `ParamExclusion.Basic` import, as your LAST step once your file builds). Task: the elementary ℤ[T] half of Route A — the degenerate/vanishing-subsum cases (Cases C and D) of the paper's Lemma 3.1 specialized to polynomial parametrizations, per SKETCH §5.2.2 and the derivation in §5.2.3; these are the branches that need NO Brownawell–Masser. In `namespace Erdos477`, importing `Erdos477.Defs` and `Erdos477.Proofs.Elementary.Basic` (NOT `ParamExclusion.Basic` — Agent 2 is editing it concurrently), produce sorry-free: (a) `polyInt_pow13_injective (p q : Polynomial ℤ) (h : p ^ 13 = q ^ 13) : p = q` — via evaluation: `(p.eval n) ^ 13 = (q.eval n) ^ 13` for every `n : ℤ`, pointwise `Erdos477.pow13_injective_proof`-style injectivity (or `Odd.pow_injective`), then `Polynomial.funext` (ℤ infinite); (b) `polyInt_pow13_eq_neg (p q : Polynomial ℤ) (h : p ^ 13 = -(q ^ 13)) : p = -q` (rewrite `-(q^13) = (-q)^13`, 13 odd, then (a)); (c) `polyInt_pow13_eq_C (p : Polynomial ℤ) (m : ℤ) (h : p ^ 13 = Polynomial.C m) : ∃ b : ℤ, p = Polynomial.C b ∧ b ^ 13 = m` (degree count `13 * p.natDegree = 0`, then `Polynomial.natDegree_eq_zero`); (d) the Case-D payoff `mem_Bset_of_pairing (c : ℤ) (p : Polynomial ℤ) (h : p ^ 13 = Polynomial.C (-c)) : c ∈ Bset` (from (c): `b^13 = -c`, so `c = (-b)^13`, witness `-b` — cf. SKETCH §5.2.2 Case D pairings 1–3, where the pair containing the constant term forces exactly this shape); (e) the generic vanishing-subsum classification over an arbitrary `AddCommGroup M`: for `x : Fin 4 → M` with `∑ i, x i = 0`, all `x i ≠ 0`, and some `I : Finset (Fin 4)`, `I.Nonempty`, `I ≠ Finset.univ`, `∑ i ∈ I, x i = 0` — conclude a two-pair splitting: `∃ I', I'.card = 2 ∧ I'.Nonempty ∧ ∑ i ∈ I', x i = 0 ∧ ∑ i ∈ I'ᶜ, x i = 0` (SKETCH §5.2.2 Case D opening: a length-1 vanishing subsum contradicts nonzeroness, length-3 forces the complementary singleton to vanish, so only 2+2 pairings survive; prove by `Finset.card` interval-cases on `I.card ∈ {1,2,3}` — `Finset (Fin 4)` case work, `decide` only on tiny numeric side goals); (f) the base-change descent helpers the spine needs to pull K-identities back to ℤ: for `K` a field of characteristic zero with the canonical `algebraMap ℤ K`, `Polynomial.map (algebraMap ℤ K)` is injective (from `Int.cast_injective`), packaged as e.g. `polyMap_int_injective (K) [Field K] [CharZero K] : Function.Injective (Polynomial.map (algebraMap ℤ K))`, plus the corollary that a vanishing pair over K descends: `(p.map φ) ^ 13 + (q.map φ) ^ 13 = 0 → p ^ 13 = -(q ^ 13)` (push the `map` through `^`/`+`/`neg`). Respect the BLUEPRINT "Cheat watch (Stage ParamExclusion)" box and USER_NOTES.md: these lemmas are the paper's OWN Case C/D steps — do NOT go further and prove `no_linear_param` by the Route-B Vandermonde argument (banned from the dependency path), and do NOT state `no_linear_param` itself. May use ✅ Elementary lemmas (`pow13_eq_neg`, `pow13_injective_proof`, `zero_mem_Bset`). `#print axioms`: standard three at most, NO custom axiom. Build gate: `lake build Erdos477.Proofs.ParamExclusion.Degenerate` clean, then add the `Erdos477.lean` import line and confirm `lake build` of the root still succeeds (if the root build fails in ANOTHER agent's in-flight file, report your own module's clean build in PROGRESS.md and note the transient). Log 🔧 claim first, then ✅ with the final lemma-name list in `Next:`.

Agent 4: OWNS `Erdos477/Proofs/BadShift/Basic.lean` (and nothing else). Task: Stage BadShift B1 reduced modulo the exclusion hypothesis — prove the conditional bound `badShift_bound_of_hexcl (c : ℤ) (hc : c ∉ Bset) (hexcl : ∀ p₁ p₂ p₃ : Polynomial ℤ, p₁ ^ 13 + p₂ ^ 13 + p₃ ^ 13 = Polynomial.C (-c) → p₁.natDegree ≤ 1 → p₂.natDegree ≤ 1 → p₃.natDegree ≤ 1 → p₁.natDegree = 0 ∧ p₂.natDegree = 0 ∧ p₃.natDegree = 0) : ∃ K : ℝ, 1 ≤ K ∧ ∀ T : ℤ, 1 ≤ T → ((Sset c T).card : ℝ) ≤ K * (T : ℝ) ^ ((5 : ℝ) / 6)` — exactly the frozen `badShift_bound` conclusion with `hexcl` as an extra hypothesis on this SUPPORT lemma only (the frozen `badShift_bound` in `Theorems.lean` stays untouched and un-restated until `no_linear_param` is ✅; next iteration discharges it as `fun c hc => badShift_bound_of_hexcl c hc (no_linear_param_proof c hc)`). Follow SKETCH §6 (P3.1 proof, Steps 0–4, + its "Lean notes") and BLUEPRINT Stage BadShift B1, building on your own iteration-1 ✅ bridge (see PROGRESS.md 2026-07-20T20:33:59Z `Next:`): Step 0 — `M := -c ≠ 0` via `Erdos477.ne_zero_of_notMem_Bset` + `neg_ne_zero`; Step 1 — for `t ∈ Sset c T` (i.e. `t ∈ Finset.Icc (-T) T` with `t^13 - c ∈ Dset`, unfold the frozen `Sset` filter) choose `(u_t, v_t)` with `t^13 - c = u_t^13 - v_t^13` (`Exists.choose`), prove `u_t ≠ v_t` (else `c = t^13 ∈ Bset`, contradicting `hc`), and define the injection `Φ t := ![u_t, -v_t, -t] : Fin 3 → ℤ` (third coordinate recovers `t`, so `Set.InjOn Φ`); Step 2 — via ✅ `Erdos477.pow13_gap_proof` (Cofactor) and `|t| ≤ T`, `1 ≤ T`: `(1/2) * max(|u_t|,|v_t|)^12 ≤ |u_t^13 - v_t^13| = |t^13 - c| ≤ (1 + |c|) * T^13`, hence every coordinate of `Φ t` has absolute value `≤ X := C_c * (T:ℝ) ^ ((13:ℝ)/12)` with `C_c := (2 * (1 + |c|)) ^ ((1:ℝ)/12)` (`C_c ≥ 1`, so `|t| ≤ T ≤ X` too; `Real.rpow` monotonicity/`Real.rpow_natCast` for the 12th-root step); Step 3–4 — feed `hexcl` to your ✅ `Erdos477.hb_diagonal_conditional` at `M := -c` to get `K` with the box count `≤ K * X ^ ((10:ℝ)/13)`, compare `(Sset c T).card` with the box's `Set.ncard` via the injection (`Set.ncard_image_of_injOn` + `Set.ncard_le_ncard` on the finite box, or `Finset.card_le_card_of_injOn` through a `Finset` coercion; use `Erdos477.diag13_eval` to translate the box membership `MvPolynomial.eval x diag13Form = -c` to `x 0^13 + x 1^13 + x 2^13 = -c`), then close with the exponent identity `(T^(13/12))^(10/13) = T^(5/6)` (`Real.rpow_mul`, `T ≥ 0`) and `K_c := K * C_c ^ ((10:ℝ)/13)` (any upper bound works; keep `1 ≤ K_c`). Respect the BLUEPRINT "Cheat watch (Stage BadShift)" box: keep `hc` live (it is what forces `u_t ≠ v_t`); the exponent must land on EXACTLY `5/6` via the honest `T^(13/12)` coordinate bound (no crude `T^2`); invoke `hb_diagonal_conditional` with the passed-in `hexcl`, never a stand-in, and never assume the count. May use: frozen `Defs.lean`, ✅ Elementary + Cofactor lemmas, your ✅ BadShift iteration-1 lemmas (import `Erdos477.Proofs.Elementary.Basic` and `Erdos477.Proofs.Cofactor.Basic` as needed). `#print axioms Erdos477.badShift_bound_of_hexcl` must show EXACTLY the standard three + `Erdos477.heath_brown_diagonal_13` (no `brownawell_masser_P1_four_term`, no `sorryAx`). Build gate: `lake build Erdos477.Proofs.BadShift.Basic` clean. Log 🔧 claim first, then ✅ with `Next:` pointing the iteration-3 agent at the one-line discharge of frozen `badShift_bound`.

## Iteration 3
Land the Route-A spine `no_linear_param` — the sole remaining hard mathematics and REVIEW.md's iteration-2 required follow-up — plus its machine-checked one-line discharge of frozen `badShift_bound`; in parallel, build the conditional Assembly layer (P5.1 pigeonhole + headline assembly, each modulo a frozen-statement hypothesis) so iteration 4 reduces to pure composition + wiring. Advances Stages ParamExclusion (completion), BadShift (completion), Assembly.

Agent 1: OWNS `Erdos477/Proofs/ParamExclusion/Spine.lean` (NEW file), the one-line import append to `Erdos477.lean`, and an append-only addition to `Erdos477/Proofs/BadShift/Basic.lean` (and nothing else — do NOT touch `Defs.lean`, `Theorems.lean`, `Solution.lean`, `Discharge.lean`, `Proofs/Assembly/**`, or any other file). PRIMARY TASK — the Route-A spine, REVIEW.md iteration-2 required follow-up: in NEW file `Erdos477/Proofs/ParamExclusion/Spine.lean` (imports: `Erdos477.Defs`, `Erdos477.Proofs.Elementary.Basic`, `Erdos477.Proofs.ParamExclusion.Basic`, `Erdos477.Proofs.ParamExclusion.Degenerate`; `namespace Erdos477`), produce sorry-free `Erdos477.no_linear_param_proof` with statement CHARACTER-EXACT to frozen `no_linear_param` (Erdos477/Theorems.lean:51–55): `(c : ℤ) (hc : c ∉ Bset) : ∀ p₁ p₂ p₃ : Polynomial ℤ, p₁ ^ 13 + p₂ ^ 13 + p₃ ^ 13 = Polynomial.C (-c) → p₁.natDegree ≤ 1 → p₂.natDegree ≤ 1 → p₃.natDegree ≤ 1 → p₁.natDegree = 0 ∧ p₂.natDegree = 0 ∧ p₃.natDegree = 0`. MANDATED PATH (USER_NOTES.md Route A = paper's Lemma 3.1 + Corollary 3.2, SKETCH §5.2.2–5.2.3, dehomogenized with `R = 1`): argue by contradiction — assume `¬(all natDegree = 0)`, so some `pᵢ` is nonconstant. Set `K := AlgebraicClosure ℚ`, `φ := algebraMap ℤ K`, `Pᵢ := pᵢ.map φ`; note `c ≠ 0` via ✅ `ne_zero_of_notMem_Bset`, so `(c : K) ≠ 0` (CharZero), and `natDegree Pᵢ = natDegree pᵢ` (`φ` injective). Mapping the hypothesis gives the four-term Polynomial-K identity `P₁^13 + P₂^13 + P₃^13 + Polynomial.C (c : K) = 0`. Case on which `Pᵢ` are zero and on vanishing proper subsums, exactly the paper's Cases A–D: (Case ≤2 nonzero powers) all `Pᵢ = 0` forces `C (c:K) = 0`, impossible; exactly one `Pᵢ ≠ 0` gives `Pᵢ^13 = C (-(c:K))`, descend to `pᵢ^13 = Polynomial.C (-c)` via ✅ `polyMap_int_injective` (push `map` through `^`, `C`, `neg`), then ✅ `mem_Bset_of_pairing` contradicts `hc`. (Case exactly two `Pᵢ, Pⱼ ≠ 0` — the paper's Case B, MUST invoke the axiom at r = 3) first dispatch vanishing proper subsums of the 3-term identity `Pᵢ^13 + Pⱼ^13 + C (c:K) = 0`: a vanishing pair `{Pᵢ^13, Pⱼ^13}` forces `C (c:K) = 0` (impossible), a vanishing pair `{P^13, C c}` forces the other power = 0 (impossible), singletons are nonzero — so no proper nonempty subsum vanishes; if both `pᵢ, pⱼ` constant then all three `p` are constant (the third is 0), closing the goal, else `e := max (natDegree Pᵢ) (natDegree Pⱼ) ≥ 1`; apply frozen axiom `Erdos477.brownawell_masser_P1_four_term` (Defs.lean:209–218) at `r := 3`, `u := ![algebraMap _ _ (Pᵢ^13), algebraMap _ _ (Pⱼ^13), algebraMap _ _ (Polynomial.C (c:K))]`, `S := ((Pᵢ*Pⱼ).roots.toFinset.image some) ∪ {none}`: S-unit hypotheses via ✅ `isSUnitP1_algebraMap` (every root of `Pᵢ` is a root of `Pᵢ*Pⱼ`) + ✅ `IsSUnitP1.pow` + ✅ `isSUnitP1_C`; not-all-constant via ✅ `algebraMap_polynomial_eq_C_iff` + `natDegree (P^13) = 13e ≥ 13 > 0`; sum-zero by pushing the polynomial identity through the ring hom `algebraMap (Polynomial K) (RatFunc K)`; no-vanishing-subsum transferred from the polynomial dispatch above (that algebraMap is injective); height via ✅ `projHeightP1_algebraMap` with constant coordinate `i₀ :=` the `C (c:K)` slot, giving `projHeightP1 u = 13*e` (sup of `{13·deg Pᵢ, 13·deg Pⱼ, 0}`); `|S|` via ✅ `card_roots_option_none_le` on `Pᵢ*Pⱼ` (`natDegree ≤ 2e`), so the axiom yields `13e ≤ 1 * (2e + 1 - 2)` — `omega`-impossible for `e ≥ 1`. (Case all three `Pᵢ ≠ 0`, no vanishing proper nonempty subsum — the paper's Case A, MUST invoke the axiom at r = 4) same recipe with `r := 4`, `u := ![⋯P₁^13, ⋯P₂^13, ⋯P₃^13, ⋯C (c:K)]`, `S := ((P₁*P₂*P₃).roots.toFinset.image some) ∪ {none}` (`natDegree ≤ 3e`, `e := max-deg ≥ 1` since some `pᵢ` nonconstant), height `13e`, coefficient `(4-1).choose 2 = 3`: `13e ≤ 3 * (3e + 1 - 2)` — impossible. (Case all three nonzero WITH a vanishing proper nonempty subsum — the paper's Case D) apply ✅ `vanishing_subsum_pairing` at `M := Polynomial K` to `x := ![P₁^13, P₂^13, P₃^13, C (c:K)]` (all entries nonzero here) to get a 2+2 split; the pair containing `C (c:K)` pairs it with some `Pₖ^13`, giving `Pₖ^13 + C (c:K) = 0`, and the same descent as above (`polyMap_int_injective` + ✅ `mem_Bset_of_pairing`) contradicts `hc`. Include the BLUEPRINT Stage-ParamExclusion guardrail example: `p₁ = X, p₂ = -X, p₃ = C b` with `∑ pᵢ^13 = C (-c)` forces `c ∈ Bset`. HARD CONSTRAINTS (BLUEPRINT "Cheat watch (Stage ParamExclusion)" + USER_NOTES.md): conclusion must be the genuine `∀ p₁ p₂ p₃ … → all natDegree = 0` with NO added hypothesis; the r = 4 AND r = 3 branches must BOTH invoke `brownawell_masser_P1_four_term` (never Mathlib's `Polynomial.abc`/Mason–Stothers — grep your file for `abc` before reporting); the Route-B Vandermonde argument is BANNED; keep the target `Polynomial.C (-c)`. `#print axioms Erdos477.no_linear_param_proof` must be EXACTLY {propext, Classical.choice, Quot.sound, Erdos477.brownawell_masser_P1_four_term} — no `heath_brown_diagonal_13`, no `sorryAx`. FINAL STEPS once `lake build Erdos477.Proofs.ParamExclusion.Spine` is clean: (i) append `import Erdos477.Proofs.ParamExclusion.Spine` to `Erdos477.lean` directly after the `ParamExclusion.Degenerate` import (only change to that file); (ii) append to `Erdos477/Proofs/BadShift/Basic.lean` (add `import Erdos477.Proofs.ParamExclusion.Spine` to its header) the discharge REVIEW.md already machine-checked to typecheck: `theorem badShift_bound_proof (c : ℤ) (hc : c ∉ Bset) : ∃ K : ℝ, 1 ≤ K ∧ ∀ T : ℤ, 1 ≤ T → ((Sset c T).card : ℝ) ≤ K * (T : ℝ) ^ ((5 : ℝ) / 6) := badShift_bound_of_hexcl c hc (no_linear_param_proof c hc)` — statement CHARACTER-EXACT to frozen `badShift_bound` (Theorems.lean:60–62); `#print axioms Erdos477.badShift_bound_proof` must be EXACTLY the standard three + BOTH `Erdos477.heath_brown_diagonal_13` AND `Erdos477.brownawell_masser_P1_four_term`; then confirm root `lake build` still succeeds. If the spine is not finished, do NOT do the final steps — report ⚠️ with the exact blocking goal instead. No `sorry`, no `native_decide`, no `axiom`. Log 🔧 claim first in PROGRESS.md per BLUEPRINT Part −1 §4, then ✅/⚠️ with `#print axioms` output in `Check:` and, on ✅, `Next:` pointing iteration 4 at wiring `no_linear_param`/`badShift_bound` into Solution.lean/Discharge.lean.

Agent 2: OWNS `Erdos477/Proofs/Assembly/Basic.lean` (and nothing else — do NOT touch `Erdos477.lean`, `Defs.lean`, `Theorems.lean`, `Solution.lean`, `Discharge.lean`, or any other `Proofs/` file). Task: the conditional Assembly layer — both stage-A payloads proved as SUPPORT lemmas whose extra hypothesis is character-exact a frozen statement (the pattern REVIEW.md iteration 2 confirmed as net progress), so that iteration 4 discharges frozen `criterion_for_B` and `erdos_477` by pure composition. Add imports `Erdos477.Proofs.Elementary.Basic` and `Erdos477.Proofs.Greedy.Basic` to the file header as needed. In `namespace Erdos477` produce sorry-free: (a) `criterion_for_B_of_bound (hbnd : ∀ c : ℤ, c ∉ Bset → ∃ K : ℝ, 1 ≤ K ∧ ∀ T : ℤ, 1 ≤ T → ((Sset c T).card : ℝ) ≤ K * (T : ℝ) ^ ((5 : ℝ) / 6)) : ∀ C : Finset ℤ, (∀ c ∈ C, c ∉ Bset) → ∃ b ∈ Bset, ∀ c ∈ C, c - b ∉ Dset` — the hypothesis is the ∀-closure of frozen `badShift_bound` (Theorems.lean:60–62) verbatim and the conclusion is CHARACTER-EXACT frozen `criterion_for_B` (Theorems.lean:72–74). Follow SKETCH §8.1 (P5.1) exactly: if `C = ∅` take `b := 0` via ✅ `zero_mem_Bset` (condition vacuous); else for each `c ∈ C` obtain `K_c ≥ 1` from `hbnd c (hC c ·)` (classical choice over the Finset), set `K := ∑_{c ∈ C} K_c` (so `K ≥ 1`, `K_c ≤ K` termwise), pick an integer `T ≥ 1` with `(K : ℝ)^6 < T` (e.g. `T := ⌈K⌉₊^6 + 1` with `Nat.le_ceil`-style bounds — any honest witness works); bound the bad set `⋃_{c ∈ C} Sset c T` (a `Finset.biUnion`) by `Finset.card_biUnion_le`: `(card : ℝ) ≤ ∑_c K_c * T^(5/6) = K * T^(5/6) < T^(1/6) * T^(5/6) = T` — for the strict step use `K < (T:ℝ)^((1:ℝ)/6)` from `K^6 < T` (`Real.rpow` monotonicity; or the sixth-power variant `(K * T^(5/6))^6 = K^6 * T^5 < T^6` per SKETCH §8.1) and close the exponent identity with `Real.rpow_natCast`/`← Real.rpow_add` (`T ≥ 1 > 0`); since `Sset c T ⊆ Finset.Icc (-T) T` (it is a filter on it) and `(Finset.Icc (-T) T).card = 2*T.toNat + 1 > T` (`Int.card_Icc`, `T ≥ 1`), the biUnion is a STRICT subset of `Finset.Icc (-T) T`, so `Finset.exists_of_ssubset` (or `card_lt_card` pigeonhole) yields `t₀ ∈ Icc (-T) T` with `t₀ ∉ Sset c T` for every `c ∈ C`; take `b := t₀^13` (`∈ Bset`, witness `t₀`) and for each `c ∈ C`: `t₀ ∉ Sset c T` with `t₀ ∈ Icc` unfolds (frozen `Sset` = `Finset.filter` with classical decidability, Defs.lean:71–72) to `t₀^13 - c ∉ Dset`, and ✅ `badShift_iff` (L0.6) converts to `c - t₀^13 ∉ Dset`, i.e. `c - b ∉ Dset`. (b) `erdos_477_of_criterion (hcrit : ∀ C : Finset ℤ, (∀ c ∈ C, c ∉ Bset) → ∃ b ∈ Bset, ∀ c ∈ C, c - b ∉ Dset) : ∃ A : Set ℤ, ∀ n : ℤ, ∃! p : ℤ × ℤ, p.1 ∈ A ∧ p.1 + p.2 ^ 13 = n` — the hypothesis is CHARACTER-EXACT frozen `criterion_for_B`'s statement and the conclusion CHARACTER-EXACT frozen `erdos_477` (Theorems.lean:79–80). Follow SKETCH §8.2 + BLUEPRINT Stage Assembly A2: apply ✅ `Erdos477.greedy_tiling_proof` (Proofs/Greedy/Basic.lean) at `B := Bset`, building its hypothesis `H` from `hcrit` by rewriting the abstract difference set `{d | ∃ x ∈ Bset, ∃ y ∈ Bset, d = x - y}` to `Dset` via ✅ `Erdos477.Dset_eq_Bset_sub` (this file); from the resulting `A` with `∀ n, ∃! ab : ℤ × ℤ, ab.1 ∈ A ∧ ab.2 ∈ Bset ∧ ab.1 + ab.2 = n`, deliver the headline `∃!` over the PAIR `(a, m)`: existence — from `ab = (a, b)` with `b ∈ Bset` obtain `m` with `b = m^13` and use `p := (a, m)`; uniqueness — for `p = (a, m)` and `p' = (a', m')` both satisfying `p.1 ∈ A ∧ p.1 + p.2^13 = n`, note `(a, m^13)` and `(a', m'^13)` both satisfy the `(a,b)`-predicate (`m^13 ∈ Bset` by witness `m`), so the `∃!`-uniqueness gives `(a, m^13) = (a', m'^13)`, hence `a = a'` (Prod.ext) and `m^13 = m'^13`, and ✅ `pow13_injective_proof` gives `m = m'` (the MANDATORY upgrade — BLUEPRINT pitfall 1). HARD CONSTRAINTS (BLUEPRINT "Cheat watch (Stage Assembly)"): `criterion_for_B_of_bound` must be `∀` finite `C ⊆ ℤ\Bset` — never a fixed/singleton `C`, no size cap; the pigeonhole must be STRICT off the honest `2T+1`-element `Finset.Icc (-T) T`; the headline conclusion must be the full `∃!` over `(a, m)` — do not stop at `(a,b)`-uniqueness or weaken `∃!` to `∃`. Do NOT state or restate frozen `criterion_for_B`, `erdos_477`, or `badShift_bound` themselves (their discharge waits for `badShift_bound_proof`, landing in Agent 1's parallel task and wired next iteration); the extra hypotheses `hbnd`/`hcrit` are permitted ONLY on these support lemmas, never on any frozen statement. Include a guardrail example deriving a contradiction from two distinct representations of a fixed `n` (per the cheat-watch box). May use anything marked ✅ in PROGRESS.md (`zero_mem_Bset`, `badShift_iff`, `Dset_neg_mem_proof`, `pow13_injective_proof`, `greedy_tiling_proof`, `Dset_eq_Bset_sub`). Build gate: `lake build Erdos477.Proofs.Assembly.Basic` clean; `#print axioms` on BOTH new lemmas must be within {propext, Classical.choice, Quot.sound} — NEITHER custom axiom may appear (the axioms enter only when the frozen-statement hypotheses are discharged next iteration). No `sorry`, no `native_decide`. Log 🔧 claim first in PROGRESS.md per BLUEPRINT Part −1 §4, then ✅ with the exact lemma names in `Next:` so iteration 4 can wire `criterion_for_B := criterion_for_B_of_bound badShift_bound_proof` and `erdos_477 := erdos_477_of_criterion (criterion_for_B_of_bound badShift_bound_proof)` plus the four Solution/Discharge entries.

## Iteration 4
Pure wiring, no new mathematics — REVIEW.md's iteration-3 required follow-up: extend Solution.lean/Discharge.lean with the four remaining restatements/gates (no_linear_param, badShift_bound, criterion_for_B, erdos_477), taking `scripts/verify.sh --all` to exit 0 and completing the Discharge & Solution stage (BLUEPRINT Part 2, "Discharge & Solution"). Every composition was machine-checked to typecheck by the iteration-3 audit.

Agent 1: OWNS `Erdos477/Solution.lean` and `Erdos477/Discharge.lean` (and nothing else — do NOT touch `Defs.lean`, `Theorems.lean`, `Erdos477.lean`, or anything under `Proofs/`). All four proof terms are ✅ per PROGRESS.md: `Erdos477.no_linear_param_proof` (Erdos477/Proofs/ParamExclusion/Spine.lean), `Erdos477.badShift_bound_proof` (Erdos477/Proofs/BadShift/Basic.lean), `Erdos477.criterion_for_B_of_bound` and `Erdos477.erdos_477_of_criterion` (Erdos477/Proofs/Assembly/Basic.lean). Two tasks. (a) In `Erdos477/Solution.lean`, `namespace Erdos477.Solution`, following the exact pattern of the six existing restatements (read the file first; add any missing imports — e.g. `Erdos477.Proofs.ParamExclusion.Spine`, `Erdos477.Proofs.BadShift.Basic`, `Erdos477.Proofs.Assembly.Basic` — matching its header style), append the four remaining restatements, each with its statement copied CHARACTER-EXACT from `Erdos477/Theorems.lean` (lines 51–55, 60–62, 72–74, 79–80): `theorem no_linear_param : <frozen statement> := Erdos477.no_linear_param_proof`; `theorem badShift_bound : <frozen statement> := Erdos477.badShift_bound_proof`; `theorem criterion_for_B : <frozen statement> := Erdos477.criterion_for_B_of_bound Erdos477.badShift_bound_proof`; `theorem erdos_477 : <frozen statement> := Erdos477.erdos_477_of_criterion (Erdos477.criterion_for_B_of_bound Erdos477.badShift_bound_proof)`. (b) In `Erdos477/Discharge.lean`, following the exact shape of the six existing gates, append the four no-drift gates: `example : @Erdos477.no_linear_param = @Erdos477.no_linear_param_proof := rfl` and `example : @Erdos477.badShift_bound = @Erdos477.badShift_bound_proof := rfl`; for the two composed theorems (which have no single `_proof` name) gate against the Solution restatements per REVIEW.md iteration 3: `example : @Erdos477.criterion_for_B = @Erdos477.Solution.criterion_for_B := rfl` and `example : @Erdos477.erdos_477 = @Erdos477.Solution.erdos_477 := rfl` (add the needed imports to Discharge.lean's header in its existing style). Do NOT alter any existing line in either file — append only, matching the shapes `scripts/verify.sh` Checks 4–5 grep for. VERIFICATION GATES (all mandatory before reporting ✅): (1) `lake build Erdos477.Solution Erdos477.Discharge` clean; (2) `#print axioms Erdos477.Solution.no_linear_param` = standard three + `Erdos477.brownawell_masser_P1_four_term` ONLY; `#print axioms` on `Erdos477.Solution.badShift_bound`, `Erdos477.Solution.criterion_for_B`, and `Erdos477.Solution.erdos_477` = EXACTLY {propext, Classical.choice, Quot.sound, Erdos477.heath_brown_diagonal_13, Erdos477.brownawell_masser_P1_four_term} — per USER_NOTES.md BOTH custom axioms MUST appear on erdos_477, no sorryAx, nothing else; (3) `scripts/verify.sh --all` must exit 0 (all five checks PASS — this is the run's completion gate); (4) `shasum -a 256 -c scripts/frozen.sha256` still passes (frozen files untouched). No `sorry`, no `native_decide`, no new `axiom`, no new declarations beyond the four restatements + four gates. Log 🔧 claim first in PROGRESS.md per BLUEPRINT Part −1 §4, then ✅ with the full `#print axioms Erdos477.Solution.erdos_477` output and the verify.sh exit code in `Check:`, and `Next:` stating the formalization is complete and ready for the closing full audit.
