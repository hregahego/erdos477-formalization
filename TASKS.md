# TASKS — Erdős 477 (thirteenth-power tiling complement) formalization

Append-only work-delegation log for 4 parallel worker agents. The Plan agent
appends one `## Iteration N` block per loop iteration. Each block has a one-line
goal then `Agent k: ...` lines (one per ACTIVE worker; inactive agents are
omitted). NEVER edit or delete an existing block.

## Iteration 1
Launch the three stages whose prerequisites are already met after SETUP: Stage D
(Greedy — the independent combinatorial MILESTONE `greedy_tiling`), Stage A
(Elementary L0.1–L0.6, the support layer every later stage cites), and Stage B
(Cofactor L1.1–L1.2, whose statements consume no Stage-A lemma). This unblocks
Stage C (needs A) and later Stage E (needs A+B+C) and Stage F (needs D+E).

Agent 1: OWNS `Erdos477/Proofs/Greedy/Basic.lean` (may add further files under `Erdos477/Proofs/Greedy/`, adding the import to `Erdos477.lean` if you do), plus `Erdos477/Solution.lean` and `Erdos477/Discharge.lean` (no other agent touches those two this iteration). GOAL: prove the frozen theorem `greedy_tiling` (BLUEPRINT Part −1 §3, statement quoted there; DO NOT edit `Erdos477/Theorems.lean` or `Erdos477/Defs.lean`) as a sorry-free declaration named `Erdos477.greedy_tiling_proof` with EXACTLY the frozen type:
  `theorem greedy_tiling_proof (B : Set ℤ) (H : ∀ C : Finset ℤ, (∀ c ∈ C, c ∉ B) → ∃ b ∈ B, ∀ c ∈ C, c - b ∉ {d : ℤ | ∃ x ∈ B, ∃ y ∈ B, d = x - y}) : ∃ A : Set ℤ, ∀ n : ℤ, ∃! ab : ℤ × ℤ, ab.1 ∈ A ∧ ab.2 ∈ B ∧ ab.1 + ab.2 = n`.
  PATH TO FOLLOW: BLUEPRINT Stage D steps D1–D4 and SKETCH §7 (L4.1 / paper's Lemma 5.1). D1 `Aseq : ℕ → Finset ℤ` by `Nat.rec` over an enumeration of ℤ (`Denumerable ℤ` / `Denumerable.eqv ℤ`), `Aseq 0 = ∅`; at step j, `Classical.dec` on whether `n_j ∈ Aseq j + B`; in the negative case apply `H` to `C_j := (Aseq j).image (fun a => n_j - a)` to get `b_j ∈ B` and insert `a_j := n_j - b_j`. D2 the two invariants by a single combined (`And`-motive) induction: (I1) `Aseq j` is Dsep-separated w.r.t. the ABSTRACT difference set `{d | ∃ x ∈ B, ∃ y ∈ B, d = x - y}`, (I2) `n_i ∈ Aseq j + B` for all `i ≤ j`; separation of the new `a_j` from an old `a` is `a_j - a = (n_j - a) - b_j ∉ D` from `H`, and the reverse direction `a - a_j ∉ D` by symmetry of the ABSTRACT difference set (prove that symmetry LOCALLY in your own file — do NOT wait on or import Stage A's `dset_neg`, which is about the concrete `Dset` and belongs to Agent 2); `a_j ∉ Aseq j` because `0` lies in the abstract difference set once `B ≠ ∅`. D3 monotone chain `i ≤ j → Aseq i ⊆ Aseq j`. D4 `A := ⋃ j, ↑(Aseq j)`: covering from (I2) + surjectivity of the enumeration; uniqueness from separation (`a + b = a' + b'`, `a ≠ a'` gives `a - a' = b' - b` in the difference set) plus cancellation for `b = b'`.
  MAY USE: nothing from other stages (Stage D depends on nothing); Mathlib freely (`Denumerable`, `Finset.image`, `Finset.induction`). Everything you need beyond Mathlib you prove in your own file.
  CHEAT-WATCH (BLUEPRINT Stage D box, BINDING): keep `B` ABSTRACT — never specialise to `Bset`; prove BOTH existence and uniqueness (`∃!` — existence alone is the headline-defeating cheat); use `H` for ALL finite `C`, not just singletons; do not assume `A` finite or that the construction terminates. No `sorry`, no `native_decide`.
  WHEN PROVED: add to `Erdos477/Solution.lean` (import `Erdos477.Proofs.Greedy.Basic`) the verbatim restatement `theorem greedy_tiling : <frozen statement> := greedy_tiling_proof` inside `namespace Erdos477.Solution`, and to `Erdos477/Discharge.lean` (same import) the gate `example : @Erdos477.greedy_tiling = @Erdos477.greedy_tiling_proof := rfl`. Then check `#print axioms Erdos477.Solution.greedy_tiling` shows ONLY `{propext, Classical.choice, Quot.sound}` — `heath_brown_diagonal_13` must NOT appear here. Log `🔧` at start and `✅`/`⚠️` at the end in `PROGRESS.md` (append-only, real `date -u` timestamps).

Agent 2: OWNS `Erdos477/Proofs/Elementary/Basic.lean` (may add further files under `Erdos477/Proofs/Elementary/`, adding the import to `Erdos477.lean` if you do). Touch NO other file. GOAL: BLUEPRINT Stage A, items A1–A6 = SKETCH §2 (L0.1, L0.2, L0.3, L0.5, L0.6) — prove all six as named, sorry-free lemmas in `namespace Erdos477`, using the frozen `Bset`/`Dset` from `Erdos477/Defs.lean` (read them; NEVER edit that file):
  - `zero_mem_B : (0:ℤ) ∈ Bset` and `zero_mem_D : (0:ℤ) ∈ Dset` (A1 / L0.1; witnesses `m = 0`, `u = v = 0`).
  - `dset_neg : ∀ d : ℤ, d ∈ Dset → -d ∈ Dset` (A2 / L0.2) — proved by SWAPPING `u,v`, not by odd-power sign manipulation.
  - `pow13_inj : Function.Injective (fun m : ℤ => m ^ 13)` (A3 / L0.3), GLOBAL on ℤ (not on a bounded range, not only for nonnegatives), via odd-power strict monotonicity (`Odd.strictMono` / `Odd.pow_right_strictMono` / `Odd.pow_left_injective` — find the exact Mathlib name with loogle/leansearch). Also export the two corollaries used downstream: `pow13_eq_iff : x ^ 13 = y ^ 13 ↔ x = y` and `pow13_eq_neg : x ^ 13 = -(y ^ 13) → x = -y`.
  - `not_B_ne_zero : ∀ c : ℤ, c ∉ Bset → c ≠ 0` (A4 / L0.5, from `zero_mem_B`).
  - `mem_D_symm_shift : ∀ c t : ℤ, c - t ^ 13 ∈ Dset ↔ t ^ 13 - c ∈ Dset` (A5 / L0.6, `dset_neg` both ways).
  - `dset_eq_sub : Dset = {d : ℤ | ∃ x ∈ Bset, ∃ y ∈ Bset, d = x - y}` (A6) — a genuine SET EQUALITY (`Set.ext`), NOT a one-way `⊆`. This is the bridge Stage F will use between the concrete `Dset` and the abstract difference set in `greedy_tiling`.
  MAY USE: only `Erdos477.Defs` + Mathlib (Stage A is the bottom layer; no other stage is ✅ yet).
  CHEAT-WATCH (BLUEPRINT Stage A box, BINDING): each of these must be a STATED, PROVED, named lemma — not `sorry`'d, not inlined ad hoc later; `pow13_inj` global; `dset_eq_sub` an equality. Confirm with `#print axioms` on each that only `{propext, Classical.choice, Quot.sound}` appear. Log `🔧` then `✅`/`⚠️` in `PROGRESS.md` (append-only, real `date -u` timestamps), and in `Next:` name each lemma so Stages C/E/F can cite them.

Agent 3: OWNS `Erdos477/Proofs/Cofactor/Basic.lean` (may add further files under `Erdos477/Proofs/Cofactor/`, adding the import to `Erdos477.lean` if you do). Touch NO other file, and do NOT import `Erdos477.Proofs.Elementary.Basic` (Agent 2 is editing it right now) — Stage B's three statements need no Stage-A lemma. GOAL: BLUEPRINT Stage B, items B1–B3 = SKETCH §4 (L1.1, L1.2), in `namespace Erdos477`, over the frozen `Qcof` from `Erdos477/Defs.lean` (`Qcof u v = ∑ i ∈ Finset.range 13, u ^ i * v ^ (12 - i)`; NEVER edit that file):
  - `qcof_factor : ∀ u v : ℤ, u ^ 13 - v ^ 13 = (u - v) * Qcof u v` (B1) — via `geom_sum₂_mul` / `Commute.geom_sum₂_mul` after matching the index convention, or by expanding the `Finset.range 13` sum (`Finset.sum_range_succ`) and `ring`. Add the guardrail `example` checking it at `u = 2, v = 1` by `norm_num`/`decide`.
  - `qcof_lower : ∀ u v : ℝ, (1/2) * (max |u| |v|) ^ 12 ≤ ∑ i ∈ Finset.range 13, u ^ i * v ^ (12 - i)` (B2 / L1.1), over ALL REALS with the constant EXACTLY `1/2`. Follow SKETCH §4 proof: (1) symmetry `Q(u,v) = Q(v,u)` by reindexing `i ↦ 12 - i` (`Finset.sum_range_reflect`), so WLOG `|v| ≤ |u|`; (2) `u = 0 ⇒ v = 0`, both sides `0`; (3) for `u ≠ 0` put `s := v/u ∈ [-1,1]`, factor out `u ^ 12 = |u| ^ 12 = (max |u| |v|) ^ 12`, reducing to `q(s) := ∑ j ∈ Finset.range 13, s ^ j ≥ 1/2` on `[-1,1]`; (4) for `0 ≤ s ≤ 1` every term is `≥ 0` and the `j = 0` term is `1`, so `q ≥ 1`; for `-1 ≤ s < 0` use `geom_sum_mul` (`(s - 1) * ∑_{j<13} s^j = s^13 - 1`, equivalently `(1 - s) q(s) = 1 - s^13`) with `s ^ 13 < 0` (13 odd) and `0 < 1 - s ≤ 2`, giving `q(s) ≥ 1/2`.
  - `pow13_gap : ∀ u v : ℤ, u ≠ v → (1/2) * (max |(u:ℝ)| |(v:ℝ)|) ^ 12 ≤ |(u:ℝ) ^ 13 - (v:ℝ) ^ 13|` (B3 / L1.2) — combine B1 (so `|u^13 - v^13| = |u - v| * Qcof u v` after casting), `|u - v| ≥ 1` for distinct integers (`Int.one_le_abs` on `u - v ≠ 0`), and B2 instantiated at the casts of `u, v` (which also gives `Qcof ≥ 0`).
  MAY USE: only `Erdos477.Defs` + Mathlib.
  CHEAT-WATCH (BLUEPRINT Stage B box, BINDING): prove B2 for ALL reals with the honest constant `1/2` — no "compactness gives some κ > 0", no unnamed constant, and no weaker/inflated bound (any slack that would later degrade the coordinate bound `T^(13/12)` to `O(T²)` destroys the `o(T)` sparsity the whole proof needs). B3's hypothesis `u ≠ v` is ESSENTIAL — do not drop it or replace it by `u, v ≥ 0`. No `sorry`, no `native_decide`. Confirm `#print axioms` on each lemma is `{propext, Classical.choice, Quot.sound}`. Log `🔧` then `✅`/`⚠️` in `PROGRESS.md` (append-only, real `date -u` timestamps).

## Iteration 2
Clear REVIEW.md's required follow-ups: fix the broken audit harness, prove the frozen
`no_linear_param` (BLUEPRINT Stage C), and land the two remaining stages *modulo their
single missing input* — Stage E (`badShift_bound` with the axiom's `hexcl` as an explicit
argument of a SUPPORT lemma) and Stage F (`criterion_holds` and `erdos_477` as support
lemmas taking the not-yet-proved input as an argument). Iteration 3 then only has to wire
the three support lemmas together into the frozen statements.

Agent 1: OWNS `Erdos477/Proofs/ParamExclusion/Basic.lean` (may add further files under `Erdos477/Proofs/ParamExclusion/`, adding the import to `Erdos477.lean` if you do), plus `Erdos477/Solution.lean` and `Erdos477/Discharge.lean` (APPEND-ONLY to those two — they already carry the `greedy_tiling` restatement + `rfl` gate from Iteration 1; never rewrite or reorder them, and no other agent touches them this iteration). Touch NO other file; NEVER edit `Erdos477/Defs.lean` or `Erdos477/Theorems.lean`.
  GOAL: BLUEPRINT Stage C (items C1–C3) = SKETCH §5.1 "Route B" (elementary Vandermonde) — prove, sorry-free, in `namespace Erdos477`:
  `theorem no_linear_param_proof (c : ℤ) (hc : c ∉ Bset) : ∀ p₁ p₂ p₃ : Polynomial ℤ, p₁ ^ 13 + p₂ ^ 13 + p₃ ^ 13 = Polynomial.C (-c) → p₁.natDegree ≤ 1 → p₂.natDegree ≤ 1 → p₃.natDegree ≤ 1 → p₁.natDegree = 0 ∧ p₂.natDegree = 0 ∧ p₃.natDegree = 0`
  (EXACTLY the frozen type of `no_linear_param`, BLUEPRINT Part −1 §3).
  PATH TO FOLLOW: SKETCH §5.1 Steps 1–3 plus its "Lean notes for Route B". C1: from `natDegree pᵢ ≤ 1` get `pᵢ = Polynomial.C aᵢ * X + Polynomial.C bᵢ` with `aᵢ = pᵢ.coeff 1`, `bᵢ = pᵢ.coeff 0` (`Polynomial.eq_X_add_C_of_natDegree_le_one` / `Polynomial.natDegree_le_one_iff`); note `natDegree pᵢ = 0 ↔ aᵢ = 0`. C2: expand `(C a * X + C b) ^ 13` by `add_pow` and read off the coefficient of `T ^ (13 - j)`, giving `(E_j) : ∑_{i} aᵢ ^ (13 - j) * bᵢ ^ j = 0` for `j = 0,…,12` (the binomial factor `C(13,j) ≠ 0` cancels) and `(E_13) : ∑_i bᵢ ^ 13 = -c`. C3: suppose `I := {i | aᵢ ≠ 0}` is nonempty; over `ℚ` set `rᵢ := bᵢ/aᵢ`, `wᵢ := aᵢ ^ 13 ≠ 0`, so `∑_{i ∈ I} wᵢ * rᵢ ^ j = 0` for `j = 0,1,2`. Prefer the SKETCH's direct 2×2 / 3×3 elimination over instantiating `Matrix.det_vandermonde`: a group of equal ratios of size 1 forces `wᵢ = 0`, contradiction; hence all `i ∈ I` share one ratio `r` and `∑_{i ∈ I} aᵢ ^ 13 = 0`. Then `|I| = 3 ⇒ c = 0 ∈ Bset`; `|I| = 2 ⇒ a₁ = -a₂` (odd-power injectivity) `⇒ p₁ = -p₂ ⇒ c = (-b₃) ^ 13 ∈ Bset`; `|I| = 1` already excluded. Every case contradicts `hc`, so `I = ∅`.
  MAY USE (all ✅ from Iteration 1): `import Erdos477.Proofs.Elementary.Basic` for `Erdos477.pow13_inj`, `Erdos477.pow13_eq_iff`, `Erdos477.pow13_eq_neg`, `Erdos477.zero_mem_B`, `Erdos477.not_B_ne_zero`; plus `Erdos477.Defs` and Mathlib. Do NOT import `Erdos477.Proofs.BadShift.Basic` or `Erdos477.Proofs.Assembly.Basic` (Agents 2 and 3 are editing those right now).
  CHEAT-WATCH (BLUEPRINT Stage C box, BINDING): the statement is `∀ p₁ p₂ p₃` with all three `natDegree ≤ 1`, concluding ALL THREE `natDegree = 0` — do not prove it only for an example triple, only for `c` in a finite set, or under an extra hypothesis such as `aᵢ ≠ 0`. Do NOT assume the ratios `rᵢ` are distinct: the EQUAL-ratio case is exactly where `c ∈ Bset` is derived and is the crux. `natDegree = 0` is the genuine "constant" (covers the zero polynomial) — do not weaken to `≤ 1`. The contradiction must land on `hc : c ∉ Bset`. `brownawell_masser_P1_four_term` must stay UNUSED (Route B only; do not import `Mathlib/NumberTheory/FLT/MasonStothers.lean`). No `sorry`, no `native_decide`.
  WHEN PROVED: APPEND to `Erdos477/Solution.lean` (adding `import Erdos477.Proofs.ParamExclusion.Basic`) the verbatim restatement inside `namespace Erdos477.Solution`: `theorem no_linear_param (c : ℤ) (hc : c ∉ Bset) : <frozen statement> := no_linear_param_proof c hc`, and APPEND to `Erdos477/Discharge.lean` (same import) the gate `example : @Erdos477.no_linear_param = @Erdos477.no_linear_param_proof := rfl`. Check `#print axioms Erdos477.Solution.no_linear_param` shows ONLY `{propext, Classical.choice, Quot.sound}` — `heath_brown_diagonal_13` and `brownawell_masser_P1_four_term` must NOT appear. Log `🔧` at start and `✅`/`⚠️` at the end in `PROGRESS.md` (append-only, real `date -u +"%Y-%m-%dT%H:%M:%SZ"` timestamps).

Agent 2: OWNS `Erdos477/Proofs/BadShift/Basic.lean` (may add further files under `Erdos477/Proofs/BadShift/`, adding the import to `Erdos477.lean` if you do). Touch NO other file — in particular NOT `Solution.lean`, NOT `Discharge.lean` (Agent 1 owns them this iteration), and NEVER `Defs.lean`/`Theorems.lean`.
  GOAL: BLUEPRINT Stage E (items E1–E4) = SKETCH §6 (P3.1 / paper's Prop 4.1) — the whole bad-shift estimate EXCEPT the exclusion hypothesis, which Agent 1 is proving in parallel. Prove, sorry-free, in `namespace Erdos477`:
  `theorem badShift_bound_of_hexcl (c : ℤ) (hc : c ∉ Bset) (hexcl : ∀ p₁ p₂ p₃ : Polynomial ℤ, p₁ ^ 13 + p₂ ^ 13 + p₃ ^ 13 = Polynomial.C (-c) → p₁.natDegree ≤ 1 → p₂.natDegree ≤ 1 → p₃.natDegree ≤ 1 → p₁.natDegree = 0 ∧ p₂.natDegree = 0 ∧ p₃.natDegree = 0) : ∃ K : ℝ, 1 ≤ K ∧ ∀ T : ℤ, 1 ≤ T → ((Sset c T).card : ℝ) ≤ K * (T : ℝ) ^ ((5 : ℝ) / 6)`
  — i.e. the frozen `badShift_bound` conclusion verbatim, with `hexcl` as an EXTRA ARGUMENT OF THIS SUPPORT LEMMA ONLY. This is explicitly allowed and is NOT a weakening of a frozen statement: the frozen `badShift_bound` itself must never gain a hypothesis; iteration 3 will define `badShift_bound_proof c hc := badShift_bound_of_hexcl c hc (no_linear_param_proof c hc)`. Do NOT restate or prove the frozen `badShift_bound` yourself this iteration (Stage C is not ✅ yet, and its file is owned by Agent 1).
  PATH TO FOLLOW: SKETCH §6 Steps 0–4 with its "Lean notes". E1 `bad_to_solution`: for `t ∈ Sset c T`, unfold membership (`Finset.mem_filter`, `Finset.mem_Icc`) to get `|t| ≤ T` and `t ^ 13 - c ∈ Dset`; by `Classical.choice`/`Exists.choose` pick `(u,v)` with `t ^ 13 - c = u ^ 13 - v ^ 13`; `u ≠ v` (else `c = t ^ 13 ∈ Bset`, contradicting `hc`); set `Φ t := (u, -v, -t)`, so `u ^ 13 + (-v) ^ 13 + (-t) ^ 13 = -c` (odd exponent) and `Φ` is INJECTIVE because the third coordinate recovers `t`. E2 `coord_bound`: from `pow13_gap` (Stage B, ✅) `(1/2) * (max |u| |v| : ℝ) ^ 12 ≤ |u ^ 13 - v ^ 13| = |t ^ 13 - c| ≤ (1 + |c|) * T ^ 13`, hence `max(|u|,|v|) ≤ C_c * T ^ (13/12)` with `C_c := (2 * (1 + |c|)) ^ (1/12 : ℝ) ≥ 1`; together with `|t| ≤ T ≤ T ^ (13/12)` every `Φ t` lies in the box `max(|x|,|y|,|z|) ≤ X`, `X := C_c * T ^ (13/12) ≥ 1`. E3 `apply_HB`: invoke `Erdos477.heath_brown_diagonal_13 (-c) hM hexcl` — `hM : (-c) ≠ 0` from `Erdos477.not_B_ne_zero` (Stage A, ✅) — passing YOUR `hexcl` argument through unchanged; obtain `K ≥ 1` with `#{(x,y,z) : sum = -c, box ≤ X} ≤ K * X ^ (10/13)`. E4: `Finset.card_le_card_of_injOn Φ` into that solution set (its `ncard`; use `Set.Finite`/`Set.ncard_le_ncard` bookkeeping as needed), then `K * X ^ (10/13) = K * C_c ^ (10/13) * T ^ (5/6)` via `Real.rpow_natCast`, `Real.rpow_mul`, `Real.mul_rpow` (`(T ^ (13/12)) ^ (10/13) = T ^ (5/6)`); set `K_c := max 1 (K * C_c ^ ((10:ℝ)/13))`.
  MAY USE (all ✅): `import Erdos477.Proofs.Elementary.Basic` (`Erdos477.pow13_inj`, `pow13_eq_iff`, `pow13_eq_neg`, `not_B_ne_zero`, `mem_D_symm_shift`, `zero_mem_B`) and `import Erdos477.Proofs.Cofactor.Basic` (`Erdos477.pow13_gap`, `qcof_lower`, `qcof_nonneg`, `qcof_factor`, `sub_pow13_eq`), plus `Erdos477.Defs` and Mathlib. Do NOT import `Erdos477.Proofs.ParamExclusion.Basic` or `Erdos477.Proofs.Assembly.Basic` (Agents 1 and 3 are editing those right now).
  CHEAT-WATCH (BLUEPRINT Stage E box, BINDING): the exponent must come out EXACTLY `(5:ℝ)/6` — any crude bound that replaces the coordinate bound `T ^ (13/12)` by `O(T ^ 2)` gives an exponent `≥ 1` and silently destroys the `o(T)` sparsity the whole proof needs. Keep `u ≠ v` in E1 — that is exactly where `c ∉ Bset` is used. `heath_brown_diagonal_13` must be applied in its CONDITIONAL form with the `hexcl` you received; do NOT introduce a second axiom, do NOT `axiom`-declare anything, and do NOT use `brownawell_masser_P1_four_term`. `#print axioms Erdos477.badShift_bound_of_hexcl` must be within `{propext, Classical.choice, Quot.sound, Erdos477.heath_brown_diagonal_13}` — no `sorryAx`, no `native_decide`. Log `🔧` then `✅`/`⚠️` in `PROGRESS.md` (append-only, real `date -u` timestamps); in `Next:` state the exact name/type of the lemma so iteration 3 can wire it to `no_linear_param_proof`.

Agent 3: OWNS `Erdos477/Proofs/Assembly/Basic.lean` (may add further files under `Erdos477/Proofs/Assembly/`, adding the import to `Erdos477.lean` if you do). Touch NO other file — in particular NOT `Solution.lean`, NOT `Discharge.lean` (Agent 1 owns them this iteration), and NEVER `Defs.lean`/`Theorems.lean`.
  GOAL: BLUEPRINT Stage F (items F1–F2) = SKETCH §8.1 and §8.2 — both assembly steps, each taking its ONE not-yet-proved input as an explicit argument of a SUPPORT lemma (the frozen `criterion_holds` / `erdos_477` themselves must never gain a hypothesis; iteration 3 will discharge the arguments). Prove, sorry-free, in `namespace Erdos477`:
  (F1) `theorem criterion_holds_of_badShift (hBS : ∀ c : ℤ, c ∉ Bset → ∃ K : ℝ, 1 ≤ K ∧ ∀ T : ℤ, 1 ≤ T → ((Sset c T).card : ℝ) ≤ K * (T : ℝ) ^ ((5 : ℝ) / 6)) : ∀ C : Finset ℤ, (∀ c ∈ C, c ∉ Bset) → ∃ b ∈ Bset, ∀ c ∈ C, c - b ∉ Dset`
  (F2) `theorem erdos_477_of_criterion (hcrit : ∀ C : Finset ℤ, (∀ c ∈ C, c ∉ Bset) → ∃ b ∈ Bset, ∀ c ∈ C, c - b ∉ Dset) : ∃ A : Set ℤ, ∀ n : ℤ, ∃! p : ℤ × ℤ, p.1 ∈ A ∧ p.1 + p.2 ^ 13 = n`
  (the conclusions are verbatim the frozen `criterion_holds` and `erdos_477` of BLUEPRINT Part −1 §3).
  PATH TO FOLLOW for F1 (SKETCH §8.1 / P5.1): if `C = ∅` take `b := 0 ∈ Bset` (`zero_mem_B`), condition vacuous. Otherwise pick `K_c` from `hBS c` for each `c ∈ C`, put `K := ∑_{c ∈ C} K_c` (over ALL of `C`), choose an integer `T ≥ 1` with `(T : ℝ) > K ^ 6`; then `(⋃_{c ∈ C} Sset c T).card ≤ ∑_{c ∈ C} (Sset c T).card ≤ K * T ^ (5/6) < T < 2 * T + 1 = (Finset.Icc (-T) T).card` (`Finset.card_biUnion_le`, `Int.card_Icc`; for the strict step argue with sixth powers: `K ^ 6 < T ⇒ (K * T ^ (5/6)) ^ 6 = K ^ 6 * T ^ 5 < T ^ 6`). Pigeonhole (`Finset.exists_mem_notMem`-style, or `Finset.card_lt_card` + `Finset.exists_of_ssubset` on `bad ∩ Icc ⊆ Icc`) gives `t₀ ∈ Finset.Icc (-T) T` with `t₀ ∉ Sset c T` for every `c ∈ C`; take `b := t₀ ^ 13 ∈ Bset`; for each `c ∈ C`, `t₀ ∈ Icc` plus `t₀ ∉ Sset c T` forces `t₀ ^ 13 - c ∉ Dset`, i.e. `c - b ∉ Dset` by `mem_D_symm_shift` (Stage A, ✅).
  PATH TO FOLLOW for F2 (SKETCH §8.2): apply `Erdos477.greedy_tiling_proof` (Stage D, ✅, in `Erdos477/Proofs/Greedy/Basic.lean`) at `B := Bset`, discharging its hypothesis `H` from `hcrit` by rewriting the abstract difference set `{d | ∃ x ∈ Bset, ∃ y ∈ Bset, d = x - y}` back to `Dset` with `Erdos477.dset_eq_sub` (Stage A, ✅). That yields `A` with a unique `(a,b)`, `b ∈ Bset`, `a + b = n`. Upgrade `(a,b)` to `(a,m)`: existence since every `b ∈ Bset` is `m ^ 13`; uniqueness of `m` from `Erdos477.pow13_inj` / `pow13_eq_iff`.
  MAY USE (all ✅): `import Erdos477.Proofs.Elementary.Basic` and `import Erdos477.Proofs.Greedy.Basic`, plus `Erdos477.Defs` and Mathlib. Do NOT import `Erdos477.Proofs.BadShift.Basic` or `Erdos477.Proofs.ParamExclusion.Basic` (Agents 2 and 1 are editing those right now) — that is why `hBS`/`hcrit` are parameters.
  CHEAT-WATCH (BLUEPRINT Stage F box, BINDING): in F1 the count comparison must be STRICT (`<`) and against the FULL two-sided `2 * T + 1 = #Finset.Icc (-T) T` — a one-sided `[0,T]` (only `T + 1` candidates) or a non-strict `≤` breaks the pigeonhole; `K := ∑_{c ∈ C} K_c` must range over ALL of `C`, not a sub-family. In F2 keep the `∃!` and the PAIR `(a,m)` uniqueness — do not drop uniqueness of `m` (that is exactly what `pow13_inj` is for) and do not restate as mere existence. Do not re-open `Defs.lean` to "adjust" `Bset`; bridge only via `dset_eq_sub`. No `sorry`, no `native_decide`, no new `axiom`. `#print axioms` on both lemmas must be within `{propext, Classical.choice, Quot.sound}` (neither may depend on `heath_brown_diagonal_13` — it enters only through the `hBS` argument later). Log `🔧` then `✅`/`⚠️` in `PROGRESS.md` (append-only, real `date -u` timestamps); in `Next:` give the exact names/types so iteration 3 can wire them.

Agent 4: OWNS `scripts/verify.sh` ONLY. Touch NO other file (no `.lean` file at all, and never `Defs.lean`/`Theorems.lean`/`scripts/frozen.sha256`/`scripts/ALLOWED_AXIOMS.txt`).
  GOAL: REVIEW.md Iteration-1 required follow-up #1 — the audit harness is currently non-functional. At `scripts/verify.sh:235-236` the two capture assignments `line=$(echo "$AX_OUTPUT" | grep "…depends on axioms")` and `noax=$(echo "$AX_OUTPUT" | grep "…does not depend on any axioms")` run under `set -euo pipefail`; when a frozen theorem is not yet restated in `Solution.lean` the `grep` exits 1 and the WHOLE SCRIPT DIES mid-Check-4 with exit code 1 — it never prints the intended `FAIL: <name> — no axiom output` line, never runs Check 5, never prints the `=== RESULT ===` summary, and writes no entry to `logs/verify_log.jsonl`. FIX: append `|| true` to both capture assignments (or otherwise neutralise `set -e` around them), so a missing theorem is reported as a per-theorem `FAIL` and the script continues to Check 5 and the RESULT summary.
  ALSO: scan the rest of `scripts/verify.sh` for the SAME pattern — any other `x=$(… | grep …)` / command-substitution capture whose non-match would abort the script under `set -e` — and neutralise those the same way. Do NOT weaken any check: the allowlist logic, the SHA-pin check, the banned-keyword check (`native_decide`/`sorry`/`admit`/`axiom`/`unsafe`), the build check and the exit-code semantics must all stay exactly as strict as they are; you are only preventing a premature abort. In particular do NOT add any theorem name to an allowlist, do NOT remove a check, and do NOT make the script exit 0 when a check fails.
  VERIFY: run `bash scripts/verify.sh` and confirm it now runs to completion, printing Checks 1–5 and the `=== RESULT ===` summary with per-theorem `FAIL: <name> — no axiom output` lines for the frozen theorems not yet in `Solution.lean` (at the time you run it, at least `badShift_bound`, `criterion_holds`, `erdos_477`), and that it still reports `greedy_tiling` as passing. NOTE other agents are editing `.lean` files in parallel, so a transient build failure in Check 3 is possible — re-run once if so and record what you saw. Log `🔧` then `✅`/`⚠️` in `PROGRESS.md` (append-only, real `date -u +"%Y-%m-%dT%H:%M:%SZ"` timestamps), quoting the final RESULT block in `Check:`.

## Iteration 3
Final wiring: compose the three already-proved support lemmas into the three remaining frozen theorems (`badShift_bound`, `criterion_holds`, `erdos_477`) and append their verbatim restatements + no-drift `rfl` gates. This closes BLUEPRINT "Discharge & Solution" (after Stages C ✅, E ✅, F ✅) and REVIEW.md Iteration-2 required follow-ups #1 and #2. No new mathematics — the composition is already verified by the auditor to elaborate.

Agent 1: OWNS exactly four files — the NEW file `Erdos477/Proofs/Wiring.lean`, plus APPEND-ONLY edits to `Erdos477/Solution.lean`, `Erdos477/Discharge.lean`, and one added `import` line in `Erdos477.lean`. Touch NO other file; NEVER edit `Erdos477/Defs.lean` or `Erdos477/Theorems.lean` (byte-frozen, SHA-pinned in `scripts/frozen.sha256`), never `scripts/ALLOWED_AXIOMS.txt`, never `scripts/verify.sh`.
  GOAL: BLUEPRINT §"Discharge & Solution" (lines 600-608) = the final wiring step. Everything you need is ALREADY PROVED and sorry-free; you are only composing it.
  (1) Create `Erdos477/Proofs/Wiring.lean` with header imports `Erdos477.Defs`, `Erdos477.Theorems`, `Erdos477.Proofs.ParamExclusion.Basic`, `Erdos477.Proofs.BadShift.Basic`, `Erdos477.Proofs.Assembly.Basic` (and `Erdos477.Proofs.Elementary.Basic` / `Erdos477.Proofs.Greedy.Basic` if needed), `namespace Erdos477`, and define exactly these three sorry-free declarations, whose statements must be VERBATIM the frozen ones in `Erdos477/Theorems.lean` (lines 31, 46, 52):
      `theorem badShift_bound_proof (c : ℤ) (hc : c ∉ Bset) : ∃ K : ℝ, 1 ≤ K ∧ ∀ T : ℤ, 1 ≤ T → ((Sset c T).card : ℝ) ≤ K * (T : ℝ) ^ ((5 : ℝ) / 6) := Erdos477.badShift_bound_of_hexcl c hc (Erdos477.no_linear_param_proof c hc)`
      `theorem criterion_holds_proof : ∀ C : Finset ℤ, (∀ c ∈ C, c ∉ Bset) → ∃ b ∈ Bset, ∀ c ∈ C, c - b ∉ Dset := Erdos477.criterion_holds_of_badShift (fun c hc => badShift_bound_proof c hc)`
      `theorem erdos_477_proof : ∃ A : Set ℤ, ∀ n : ℤ, ∃! p : ℤ × ℤ, p.1 ∈ A ∧ p.1 + p.2 ^ 13 = n := Erdos477.erdos_477_of_criterion criterion_holds_proof`
    (If a binder/η mismatch appears, adjust ONLY the argument shape — e.g. `fun c hc => …` — never the statement.)
  (2) Add `import Erdos477.Proofs.Wiring` to `Erdos477.lean` (append after the existing `Erdos477.Proofs.*` imports, before `Erdos477.Solution`); leave every other line of that file unchanged.
  (3) APPEND to `Erdos477/Solution.lean` — do NOT rewrite or reorder its existing content (the Iteration-1 `greedy_tiling` and Iteration-2 `no_linear_param` entries must stay byte-identical): `import Erdos477.Proofs.Wiring` in the header, and inside `namespace Erdos477.Solution` the three VERBATIM restatements `theorem badShift_bound (c : ℤ) (hc : c ∉ Bset) : … := Erdos477.badShift_bound_proof c hc`, `theorem criterion_holds : … := Erdos477.criterion_holds_proof`, `theorem erdos_477 : … := Erdos477.erdos_477_proof`, each statement copied character-for-character from `Erdos477/Theorems.lean` (`badShift_bound` :31, `criterion_holds` :46, `erdos_477` :52).
  (4) APPEND to `Erdos477/Discharge.lean` (same import; existing two `example`s untouched) the three no-drift gates: `example : @Erdos477.badShift_bound = @Erdos477.badShift_bound_proof := rfl`, `example : @Erdos477.criterion_holds = @Erdos477.criterion_holds_proof := rfl`, `example : @Erdos477.erdos_477 = @Erdos477.erdos_477_proof := rfl`. These MUST elaborate — a `rfl` failure means the type drifted; fix your proof term, never the frozen statement.
  MAY USE (all ✅ and sorry-free, per PROGRESS.md): `Erdos477.no_linear_param_proof` (Stage C, `Proofs/ParamExclusion/Basic.lean:173`), `Erdos477.badShift_bound_of_hexcl` (Stage E, `Proofs/BadShift/Basic.lean:204`), `Erdos477.criterion_holds_of_badShift` (Stage F1, `Proofs/Assembly/Basic.lean:58`), `Erdos477.erdos_477_of_criterion` (Stage F2, `Proofs/Assembly/Basic.lean:129`), plus Stage A/B/D lemmas if incidentally needed.
  SKETCH: §6 (P3.1) feeding §8.1 (P5.1) feeding §8.2 (Theorem 1.1) — the composition order is exactly the dependency chain `no_linear_param → badShift_bound → criterion_holds → erdos_477`.
  CHEAT-WATCH (BLUEPRINT Stage E + Stage F boxes and §"Discharge & Solution", BINDING): the frozen `badShift_bound` must NOT inherit `hexcl` as a hypothesis — `hexcl` is discharged HERE by applying `no_linear_param_proof c hc`, never assumed and never added to a frozen statement. Exponent stays literally `(5:ℝ)/6`; `criterion_holds` stays `∀ C`; `erdos_477` keeps the `∃!` over the PAIR. No `sorry`, no `native_decide`, no `admit`, no `unsafe`, no new `axiom` declaration, and do NOT use `brownawell_masser_P1_four_term`.
  VERIFY AND REPORT: run `lake build`, then `bash scripts/verify.sh` and require `=== RESULT: PASS (0 issue(s)) ===` with Check 4 passing all five theorems; also run `#print axioms Erdos477.Solution.erdos_477` and require exactly `[propext, Classical.choice, Erdos477.heath_brown_diagonal_13, Quot.sound]` (no `sorryAx`, no `brownawell_masser_P1_four_term`), and `#print axioms Erdos477.Solution.criterion_holds` / `Erdos477.Solution.badShift_bound` likewise within the allowlist. Log `🔧` at the start and `✅`/`⚠️` at the end in `PROGRESS.md` (APPEND-ONLY, real `date -u +"%Y-%m-%dT%H:%M:%SZ"` timestamps), quoting the full `verify.sh` RESULT block and the `#print axioms` lines in `Check:`.
